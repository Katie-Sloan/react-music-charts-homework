import React from "react";

const SongItem = ({song}) => {

    // if(!loaded){
    //     return <p>Loading...</p>
    // }

    return(
        song["im:name"]["label"]
        // songs["feed"]["entry"].indexOf(song)["im:name"]
    )
}

export default SongItem